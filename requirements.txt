fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
segno>=1.6
python-multipart>=0.0.9
pytest>=8.0
httpx>=0.27
# Postgres (Supabase) — активен только при DATABASE_URL, иначе SQLite (stdlib)
psycopg[binary]>=3.1,<4
psycopg-pool>=3.2,<4
