# fly.toml app configuration file generated for yummy-astana on 2026-08-01T12:11:37+05:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'yummy-astana'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[env]
  PORT = '8000'
  YUMMY_CORS_ORIGINS = 'https://wpalish.github.io'
  YUMMY_ENFORCE_AUTH = '1'
  YUMMY_PUBLIC_URL = 'https://wpalish.github.io/yummy'

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '15s'
    method = 'GET'
    path = '/health'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'
