.venv/
__pycache__/
*.pyc
.pytest_cache/
*.db
.DS_Store
backups/
# все env-файлы с секретами (.env, .env.local, .env.production …),
# кроме шаблона .env.example — иначе пароль БД уедет в git
.env
.env.*
!.env.example
graphify-out/

# Продажи: база лидов и рабочие материалы — НЕ публикуются (репо публичный).
# Внутри контакты и заметки по заведениям; в открытом доступе им не место.
data/leads/
sales/
