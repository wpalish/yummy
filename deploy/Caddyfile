{$YUMMY_DOMAIN} {
    encode zstd gzip
    reverse_proxy app:8000 {
        health_uri /health
        health_interval 30s
        health_timeout 5s
    }
    header {
        -Server
    }
    log {
        output stdout
        format json
    }
}
